CONFIG_SPI=y
CONFIG_INPUT=y
CONFIG_ZMK_POINTING=y

CONFIG_ZMK_EXT_POWER=y

# PMW3610 driver is auto-enabled by device tree when trackball node is present
# Removed explicit CONFIG_PMW3610=y to avoid Kconfig errors if vendor prefix isn't recognized
